* 项目中的所有视频都打标完成后，页面上会出现数据下载按钮，点击可以下载项目完* 整的数据。
* 下载数据格式和内容要求：
* 下载的数据表是一张包含此项目中所有视频数据的宽表。
宽表的字段有：视频标题，视频URL，创建项目时用户指定分析指标，所有的标签名字（每个名字是一个字段。命名规则是“标签类别名：标签名“）
宽表中每条记录的标签字段的取值使用“one-hot”的方式赋值。比如该视频的标签A是匹配中的，那么记录的值就是1，否则就是0
数据下载成csv格式